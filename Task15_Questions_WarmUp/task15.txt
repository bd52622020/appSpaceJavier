1. What is the purpose local or local[*] in a scala code ?
When you create a Scala session this parameter means that you will run Spark in local and taking all the procesors that your machine has.

2. What is the difference between map and flatMap ?
Map creates an array in each iteration while in flatMap it can return a list of all the elements.

3. What are two fundamental methods of creating a RDD in spark ?
There are two ways to create RDDs: parallelize an existing collection and reference an external dataset.

4. What is the difference between map and mapValues ?
Map iterate on the entire line and mapValues only in the values.

5. What transformation helps us summarize data according to a key ?
To summarize data if you want to sum, you can use reduceByKey.

