package com.javier.warmup;

public class ExtractTags {

//	Write a Python program to extract and display all the header tags from  
	// I need to convert this project into Maven and I do not have time, so Im going to put the steps here
	
	// Inject parser dependency, for example jsoup
	//Document doc = Jsoup.connect("en.wikipedia.org/wiki/Main_Page").userAgent("Mozilla").get();
	//Elements elementsObj = doc.select("h1, h2, h3, h4, h5, h6");
	
	// :)
	
}
